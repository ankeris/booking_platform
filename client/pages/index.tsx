import Head from "next/head";

const Home = () => (
    <div>
        <Head>
            <title>Create Next App</title>
            <link rel="icon" href="/favicon.ico" />
        </Head>

        <main>
            <h1>next app</h1>
        </main>

        <footer></footer>
    </div>
);

export default Home;
